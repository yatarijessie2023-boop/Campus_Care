PORT=3000
# MySQL connection
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=fcu_campus_report
DB_SOCKET=
# JWT signing secret
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRES_IN=8h
# Frontend origin for CORS
FRONTEND_URL=http://localhost:5173
# Optional mail settings for status notifications
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM="FCU Campus Report <no-reply@example.com>"
